name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'mantiq/**'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build
        env:
          HOME: /root
        run: |
          cd mantiq
          chmod +x mvnw
          ./mvnw package -DskipTests -q

      - name: Restart
        run: |
          cp mantiq/target/mantiq-0.0.1-SNAPSHOT.jar /root/mantiq.jar
          systemctl restart mantiq
