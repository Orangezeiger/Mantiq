name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'mantiq/**'

jobs:
  deploy:
    runs-on: self-hosted
    env:
      HOME: /root
      MAVEN_USER_HOME: /root/.m2
    steps:
      - uses: actions/checkout@v4

      - name: Install Maven
        run: which mvn || apt-get install -y maven -q

      - name: Build
        run: cd mantiq && mvn package -DskipTests -q

      - name: Restart
        run: |
          cp mantiq/target/mantiq-0.0.1-SNAPSHOT.jar /root/mantiq.jar
          systemctl restart mantiq
